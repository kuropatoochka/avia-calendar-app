# Пример переменных окружения (скопируйте в .env при необходимости)
# PostgreSQL: драйвер psycopg 3 (без миграций в приложении — только DML из кода)
# DATABASE_URL=postgresql+psycopg://analytics:analytics@127.0.0.1:5433/analytics
# ANALYTICS_DEBUG=false
